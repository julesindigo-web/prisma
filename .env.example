# PostgreSQL connection string (Vercel Postgres / Neon / Supabase / local)
DATABASE_URL="postgresql://user:password@localhost:5432/prisma_smk"

# NextAuth
NEXTAUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Seed admin (System Owner) account created by prisma/seed.ts
SEED_SO_EMAIL="so@prisma.local"
SEED_SO_PASSWORD="ChangeMe123!"
